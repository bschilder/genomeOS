# CHELSA annual consistency evidence delivery

Advances #189 WP3/WP7 and the existing PR240 source-qualification note. This is a documentation/figure delivery of a completed bounded experiment, not a new model or source admission. Binding scientific evidence is the controller-retained annual audit and sibling daily source trace, identified by logical record name and SHA-256 in the public aggregate. Those evidence files are inputs, never a set of files to stage wholesale. The public note and aggregate JSON must contain sufficient source identities, methods, results and limits to stand alone.

## Scientific contract

1. Claim: the exact stored-integer floor relationship between twelve current CHELSA monthly precipitation files and bio12 fails in a small, fully counted subset, while the annual physical scale and generating lineage remain unqualified.
2. Acceptance: a public aggregate evidence file reconciles exactly to the retained902,016,000-grid-cell audit and all13 source hashes; a standalone script regenerates the native-block diagnostic map/histogram from that aggregate without raster access. The note retains failed attempts, post-hoc selection and strict refusal, rather than implying a passed scientific admission.
3. Component/interface: a research JSON artifact, standalone offline plot CLI and companion note in the existing PR240 branch. P0/P1, fitting, serving, source metadata contracts and scientific eligibility are unchanged.
4. Assumptions/refusals/consumers: these are modeled climate stored integers and native source masks, not measured genetics, land truth, physical calibration or independent replicates. No threshold, scale, location, source or outcome substitution. Research readers use the note/figure; no fitter consumes the data.

## Global Constraints

- Use the existing `research/global-af-terrain-qualification` worktree, exact initial head `9920901e780a600b031a86b806309e7e3f949c1f`. Preserve all previous commits and worktrees. No merge, rebase, reset, force push, network operation or external publication by the implementer.
- Keep902,016,000 grid cells,902,015,154 comparable,846 excluded,901,192,324 exact matches,822,830 exact-floor failures. Residual10*annual_raw-sum(monthly_raw) spans-10..0; all formula failures are-10. Native validity includes land and ocean; no area weighting.
- The monthly explicit binary64 scale0.1 and rational raw division by10 are distinct arithmetic statements. Annual SCALE/OFFSET are absent; GDAL1/0 are unqualified reader defaults. Do not assert a verified generating algorithm, physical annual scale, climate accuracy or genetic gain.
- Preserve the original fixed-point selection and both failed attempts. Packing hypotheses were frozen after monthly values but before annual pixels. Whole-grid formula selection was post hoc to six fixed-cell results. Worst-cell second decoding is a selected diagnostic, not independent discovery.
- Source data terms checked as CC0 are distinct from author code GPLv3. Publish only whitelisted aggregate data, source metadata/hashes, owned plotting code and own figure/prose. Never stage raw rasters, downloaded source/PDF/HTML, auth/history/private stores, local absolute paths, private audit files or genomic/participant rows. Do not copy vendor code.
- Observations and inferred surfaces remain separate; this figure is explicitly climate storage-consistency evidence. Counts of excluded source cells stay visible. Color ramp increases with failure fraction, native edge blocks use true affine extents, no invented coastline/land mask or spatially smoothed evidence.
- Status stays automated_proposal/pending and scientific model/publication eligibility false. No new dependency, production schema, API, fitter, serving, genetic-data or data-access change.
- Do not change a source report or frozen experiment result to make the figure pass. Preserve the original root audit and every previous public note's historical scope.
- All repository changes go through this existing branch/PR. Run focused artifact checks, mandatory smoke, Ruff, frozen contracts, module size, privacy and full pytest before commit. Retain complete logs and warning text in the controller-retained task ledger; inspect staged paths before each commit. Do not rerun old scientific experiments or download anything.

## Task 1: Deliver a reproducible annual-consistency note and figure

**Files:**
- Create docs/research/chelsa-annual-consistency-2026-09-11.md.
- Create docs/research/chelsa-annual-consistency-2026-09-11.json.
- Create scripts/plot_chelsa_annual_consistency.py.
- Create docs/figures/chelsa_annual_consistency.png.
- Modify docs/research/chelsa-decoding-checks-2026-09-10.md with a clearly dated follow-up/link, preserving original audit limitations rather than rewriting history.
- Include this controller-authored plan file in the commit. No other tracked paths.

Read AGENTS.md and the project context it requires before writing. Use the exact controller-retained result records enumerated by logical name and SHA-256 in the public aggregate, together with the separately identified daily producer trace. The retained plotting diagnostic is owned code that may be adapted; do not copy its local-root or raw-file assumptions into the public script.

**Public evidence contract:** JSON schema marker chelsa-annual-consistency-v1; explicit automated_proposal/pending status and both scientific eligibility flags false. Include source release/period, the exact affine/CRS/dimensions, each13 asset's role/public URL/size/SHA256 and explicit packing state, and a copy of the whole-grid result's count fields, signed histogram and every block summary. Include the original plan/result/script hashes and scoped runtime/decoder identities. Include the selected worst-cell raw values and independent-decoder scope. Whitelist fields and inspect every resulting key/value; omit local paths, machine/process identifiers, personal context, raw source bodies and large raster bytes. The complete structured artifact should be under5MiB. Clearly say this aggregate permits figure reproduction, not independent full-raster replication by itself. Record original source-file hashes separately from this newly serialized public artifact.

**Plot interface:** `python scripts/plot_chelsa_annual_consistency.py --evidence docs/research/chelsa-annual-consistency-2026-09-11.json --out NEW_PNG`. No network/raster read or implicit download. Typed functions and module docstring cite Atlas design §§4,7. Validate marker/eligibility, grid/block tiling, integer totals, histogram/failure/min/max accounting and source-role identities before opening output. Refuse an existing output. Reuse the inspected two-panel figure semantics: native-block exact-floor-failure fraction and signed raw residual histogram. Show comparable/excluded/failure counts and raw-integer formula, native masks include ocean, annual physical scale unverified, no genetic skill claim. Use readable low-to-high color and true clipped edge-block geometry. Deterministic scientific plotting; no stochastic path or fabricated SEED is needed. No general plotting framework or science-module abstraction.

**Note:** lead with the falsified exact formula and retained uncertainty, then scientific contract, exact source IDs/hashes and current source-intended amounts, preserved failure/selection chronology, selected decoding/whole-grid results, figure interpretation, exact reproducer and remaining source/genetic gates. Use precise primary source links already present in retained evidence. Preserve the source revision/date mismatch and distinguish daily code from the unknown global monthly generator. Report source/feature eligibility truthfully; do not certify human review. Embed committed PNG with the repository raw URL and provide the aggregate/CLI links. This is an own prose synthesis; do not reproduce copyrighted source bodies. No author contact or issue comments.

**Verification:** generate PNG from public JSON and visually inspect it. Verify every copied aggregate/block value and13source hashes against controller-retained sources; PNG counts/limits must agree. Retain a meaningful negative check by altering a copied total or histogram in a temporary evidence file: CLI must exit nonzero before creating output. Confirm the existing-output refusal preserves bytes. This small plotting/documentation task does not need an artificial failing pytest test or tests mirroring plotting internals. Run existing required gates/full suite once on the final source and retain exact outputs/warnings. The unchanged locked project virtual environment and sibling Ruff may be reused with `PYTHONPATH=.` and `PYTHONDONTWRITEBYTECODE=1` plus task-specific temporary caches; verify imports resolve this worktree. Do not install/change the environment.

Before committing, inspect the six staged paths and privacy output. Commit `docs: retain CHELSA annual consistency evidence` with a body saying `Advances #189` (do not close #189). Update this plan's completion checks truthfully. No push/PR update/merge: the controller handles delivery after task and final review. Write the full controller-retained task report, including commands, outputs/warnings, source mappings, generated artifact hashes, limitations and commit. Return only DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED, head, one-line verification and concerns. Do not spawn subagents.


- [x] Public aggregate reconciles to all retained source records and block totals.
- [x] Plot reproduces the figure; malformed aggregate and existing-output checks pass.
- [x] Scientific note and dated companion update retain every material limitation.
- [x] All required gates/full suite, exact warning retention, staged privacy and commit complete.
