{
  "coerce": true,
  "columns": {
    "naming_citation": {
      "dtype": "string[pyarrow]"
    },
    "normalized_variant_id": {
      "dtype": "string[pyarrow]",
      "str_matches": "^chr(?:[1-9]|1[0-9]|2[0-2]|X|Y|MT)-[1-9][0-9]*-[ACGT]-[ACGT]$|^$"
    },
    "notes": {
      "dtype": "string[pyarrow]"
    },
    "printed_alleles": {
      "dtype": "string[pyarrow]",
      "str_matches": "^[ACGT]/[ACGT]$"
    },
    "printed_convention": {
      "dtype": "string[pyarrow]",
      "meaningful_text": {
        "error": "this value resembles a spelling of absence (-, n/a, na, none, not reported, null, tbd, unknown). It has been preserved for human review. Confirm whether the source intended a real literal value or missing metadata; missingness must use the field's explicit recorded state rather than looking like trusted metadata.",
        "raise_warning": true
      },
      "non_blank_text": {
        "error": "required text must contain at least one non-whitespace character"
      }
    },
    "reference_resource": {
      "dtype": "string[pyarrow]"
    },
    "refusal_reason": {
      "dtype": "string[pyarrow]"
    },
    "resolved_at": {
      "dtype": "string[pyarrow]",
      "meaningful_text": {
        "error": "this value resembles a spelling of absence (-, n/a, na, none, not reported, null, tbd, unknown). It has been preserved for human review. Confirm whether the source intended a real literal value or missing metadata; missingness must use the field's explicit recorded state rather than looking like trusted metadata.",
        "raise_warning": true
      },
      "non_blank_text": {
        "error": "required text must contain at least one non-whitespace character"
      }
    },
    "reviewed_by": {
      "dtype": "string[pyarrow]",
      "meaningful_text": {
        "error": "this value resembles a spelling of absence (-, n/a, na, none, not reported, null, tbd, unknown). It has been preserved for human review. Confirm whether the source intended a real literal value or missing metadata; missingness must use the field's explicit recorded state rather than looking like trusted metadata.",
        "raise_warning": true
      },
      "non_blank_text": {
        "error": "required text must contain at least one non-whitespace character"
      }
    },
    "rsid": {
      "dtype": "string[pyarrow]",
      "str_matches": "^rs[1-9][0-9]*$|^$"
    },
    "status": {
      "dtype": "string[pyarrow]",
      "isin": [
        "resolved",
        "unresolved"
      ]
    },
    "strand": {
      "dtype": "string[pyarrow]",
      "str_matches": "^(?:plus|minus)$|^$"
    },
    "strand_evidence": {
      "dtype": "string[pyarrow]"
    },
    "variant_id": {
      "dtype": "string[pyarrow]",
      "meaningful_text": {
        "error": "this value resembles a spelling of absence (-, n/a, na, none, not reported, null, tbd, unknown). It has been preserved for human review. Confirm whether the source intended a real literal value or missing metadata; missingness must use the field's explicit recorded state rather than looking like trusted metadata.",
        "raise_warning": true
      },
      "non_blank_text": {
        "error": "required text must contain at least one non-whitespace character"
      },
      "unique": true
    },
    "verification_status": {
      "dtype": "string[pyarrow]",
      "isin": [
        "verified",
        "pending"
      ]
    }
  },
  "drop_invalid_rows": false,
  "name": "variant_normalization",
  "schema_type": "dataframe",
  "strict": true
}
